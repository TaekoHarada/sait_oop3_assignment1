readMe.txt: instructions on how to install and use your sorting program.

Allocate Sort.jar, testData folder and command.bat file (for macOS command.sh) at the same directory.

For Windows:


For MacOS:
	1. Open the terminal.
	
	2. Move to the sorting program directory.
	(e.g. % cd [the directory path])
	
	4. Make the command.sh executable.
	(% chmod +x command.sh) 
	
	3. Run the command file. 
	(% ./command.sh) 
	
	
	